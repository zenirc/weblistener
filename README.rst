Weblistener
===========

This is a weblistener that takes a JSON body posted at it and puts it into a Redis Pub/Sub channel.

Written to work with ZenIRCBot services but isn't limited to that use case.